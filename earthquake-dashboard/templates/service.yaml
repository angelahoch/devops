apiVersion: v1
kind: Service
metadata:
  name: {{ include "earthquake-dashboard.fullname" . }}
  labels:
    {{- include "earthquake-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "earthquake-dashboard.selectorLabels" . | nindent 4 }}
